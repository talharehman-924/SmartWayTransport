// Hardcoded admin credentials — only ONE admin, no signup for admin
// Change these values or set them via environment variables

export const ADMIN_EMAIL = process.env.ADMIN_EMAIL || 'admin@smartway.com';
export const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || 'Admin@12345';
